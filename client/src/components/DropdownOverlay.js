import styled from 'styled-components';

const DropdownOverlay = styled.div`
  height: 100%;
  width: 100%;
  position: fixed;
  z-index: 99;
  top: 0;
  left: 0;
  overflow-x: hidden;
`;

export default DropdownOverlay;
